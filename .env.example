# Optional if you use “Configure API key” inside the app (stored in browser local storage).
# Otherwise: copy this file to .env next to server.js.
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional
# PORT=3456
# HOST=0.0.0.0
