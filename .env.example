# Copy to .env next to server.js if you want to override defaults (never commit .env).
# AI uses each user’s own Anthropic key from the app — not this file.

# PORT=3456
# HOST=0.0.0.0

# Optional: classic PAT with "repo" scope — only for `npm run dist:publish` (never ship in the app).
# GH_TOKEN=ghp_...
